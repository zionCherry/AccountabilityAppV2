package com.example.androidappv2;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class find_button_page extends AppCompatActivity {
    //this is the function definition for the find button page
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);// creates the new state
        setContentView(R.layout.activity_find_button_page);// is responsible for actually changing what is viewed
    }
}